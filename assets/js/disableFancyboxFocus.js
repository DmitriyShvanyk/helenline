(function(){
    'use strict';

    // fancybox places focus back on the trigger element after closing the gallery and 
    // sometimes carousel/slider scripts are sensitive to this feature. To disable that, 
    // set backFocus:false
    
    $.fancybox.defaults.backFocus = false; 

})();